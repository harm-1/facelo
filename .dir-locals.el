;;; Directory Local Variables
;;; For more information see (info "(emacs) Directory Variables")

((python-mode . ((pytest-global-name . "docker-compose run --rm backend pytest")
                 (projectile-project-test-cmd . "docker-compose run --rm backend pytest backend/tests/"))))
